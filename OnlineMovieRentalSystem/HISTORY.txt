CREATE TABLE HISTORY(
        CUSTOMER VARCHAR(50) PRIMARY KEY,
	MOVIE VARCHAR(50),
	RENTAL_DATE VARCHAR(50),
	RETURN_DATE VARCHAR(50),
	PRICE DECIMAL(6, 2),
	PAYBACK DECIMAL(6, 2),
	CHANGE DECIMAL(6, 2)
        FOREIGN KEY (CUSTOMER) REFERENCES LOGIN_USER(USERNAME) ON DELETE CASCADE
);